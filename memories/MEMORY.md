- Env: tradedesk-dc-export at project .venv/bin. Stocks: --price-divisor 1000 --commit-partial-after-days 0; cache path month 0-based, jetta API 1-based. "Last week" = Mon–Fri of prior week; RSI tasks output workspace/RSI-<TICKER>.csv (timestamp,close,rsi_14_wilder), script workspace/rsi_wilder_nvda.py pattern.
- Dukascopy throttling: datafeed AND jetta can 429 simultaneously. Working fallback: S3 bucket cfg-public-proper-wallaby (eu-west-1, Requester Pays, aws CLI+creds on host) — s3://…/<SYMNORM>/YYYY/MM0/DD_ticks.bi5, daily LZMA bi5, records >IIIff (ms since DAY start UTC, ask, bid, avol, bvol), same divisor 1000. Details in tradedesk-dukascopy skill §"datafeed is blocked" 2b.
- Env: tradedesk-dc-export at project .venv/bin. Stocks: --price-divisor 1000 --commit-partial-after-days 0; cache path month 0-based, jetta API 1-based. "Last week" = Mon–Fri of prior week; indicator tasks output workspace/<IND>-<TICKER>.csv (timestamp,close,<ind>). Pipeline for blocked datafeed: aws s3 → workspace/decode_s3_<ticker>.py (daily bi5 → export/<SYM>_1MIN_bid.csv) → workspace/ema<N>_<ticker>.py or rsi_wilder.py (pattern: decode_s3_nvda.py, ema9_nvda.py). Yahoo 1-min chart API for cross-checks.
